#include <stdio.h>
#include <string.h>

int longestPalindrome(char *s)
{
      int length = strlen(s);
}

int main()
{

      char s[];
      scanf("%99s", s);
      longestPalindrome(s)

          return 0;
}
// liet ke m phan tu tu tap N = {1, 2, ....,n }

#include <stdio.h>
int m, n, s[1000000];
void Try(k)
{
      for (int v = s[k - 1] + 1; v <= n - m + k; v++)
      {
            }
}

int main()
{
      scanf("%d", m);
      scanf("%d", n);

      return 0;
}